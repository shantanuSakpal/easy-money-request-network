export type RecipientType = {
  name: string;
  email: string;
  walletAddress: string;
  teamName: string;
  amount: number;
  address: string;
  city: string;
  country: string;
  postalCode: number;
  notes: string;
  status: string;
};
